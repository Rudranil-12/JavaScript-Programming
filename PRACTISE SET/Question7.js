// for a given no of array of nnumbers, print the squre of each value using for each loop

let arr = [1,2,3,4,5];

let squrecalculate = arr.forEach((val) => {
    console.log(val * val);
    
})

val.forEach(squrecalculate);
